{
  "name": "lineage-atlas",
  "version": "1.0.0",
  "private": false,
  "description": "Search a column, see where it comes from and everything it feeds. Local lineage docs for data pipelines: import a dbt project in one command, let an AI agent document it, review its work on a graph.",
  "bin": {
    "lineage-atlas": "bin/lineage-atlas.mjs"
  },
  "files": [
    "bin/",
    "server/dist/",
    "web/dist/",
    "docs/",
    ".claude/skills/",
    "CHANGELOG.md"
  ],
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run check:version && npm run check:docs && npm run build -w server && npm run build -w web",
    "start": "npm run start -w server",
    "seed": "npm run seed -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "check:version": "node scripts/check-version.mjs",
    "check:docs": "node scripts/check-docs.mjs",
    "pipelines": "npm run pipelines -w server",
    "export": "npm run export -w server --",
    "import": "npm run import -w server --",
    "validate": "npm run validate -w server --",
    "from-dbt": "npm run from-dbt -w server --",
    "summary": "npm run summary -w server --",
    "scan": "npm run scan -w server --",
    "status": "npm run status -w server --",
    "install-skill": "npm run install-skill -w server --",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "express": "^4.22.2",
    "nanoid": "^5.0.9"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=24"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "esbuild@0.25.12": true
  },
  "license": "MIT",
  "keywords": [
    "dag",
    "data-lineage",
    "dbt",
    "data-engineering",
    "ai-agents",
    "claude-code",
    "data-pipeline",
    "documentation",
    "data-catalog",
    "metadata",
    "graph",
    "observability"
  ],
  "homepage": "https://github.com/eesa-syed/lineage-atlas#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/eesa-syed/lineage-atlas.git"
  },
  "bugs": {
    "url": "https://github.com/eesa-syed/lineage-atlas/issues"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
